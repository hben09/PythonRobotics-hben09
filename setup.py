from setuptools import setup, find_packages

setup(
    name="PythonRobotics",
    version="1.0.0",
    packages=find_packages(),
    install_requires=[
        # Core dependencies are in requirements/requirements.txt
    ],
)
